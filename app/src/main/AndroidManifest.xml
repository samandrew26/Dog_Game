<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.dog_game">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".search_breeds"
            android:label="@string/title_activity_search_breeds"
            android:theme="@style/AppTheme.NoActionBar"></activity>
        <activity
            android:name=".id_dog"
            android:label="@string/title_activity_id_dog"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".id_breed"
            android:label="@string/title_activity_id_breed"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>